logging:
  config: classpath:logback.xml

server:
  port: 8088

spring:
  profiles:
    active: dev
  application:
    name: message-netty-service


management: #暴露所有
  endpoints:
    web:
      exposure:
        include: "*"

