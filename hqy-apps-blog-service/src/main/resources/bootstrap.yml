logging:
  config: classpath:logback.xml

server:
  port: 8787

spring:
  application:
    name: apps-blog-service
  profiles:
    active: test

management:
  endpoints:
    web:
      exposure:
        include: "*"